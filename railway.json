{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "streamlit run railway_app.py --server.port $PORT --server.address 0.0.0.0",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}